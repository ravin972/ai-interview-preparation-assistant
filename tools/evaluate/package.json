{
  "name": "@kit/evaluate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Batch evaluator CLI. Uses the same runPipeline() as the API. No database, no auth.",
  "main": "./src/cli.ts",
  "dependencies": {
    "@kit/core": "*"
  }
}
